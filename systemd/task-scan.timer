# 可選：webhook 漏接的對帳保底，每小時拉一次 task-dispatch.service。
# 沒設門鈴的機器，這就是唯一的自動喚醒（level-triggered 的降級模式：慢，但不漏）。
# Persistent=true 是 WSL2 的命脈：機器睡過排程點，醒來補跑（journal 踩過的坑）。
[Unit]
Description=backlog 對帳輪詢 timer（門鈴漏接的保底）

[Timer]
OnCalendar=hourly
Persistent=true
Unit=task-dispatch.service

[Install]
WantedBy=timers.target
