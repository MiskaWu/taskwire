# taskwire 無頭線（全域指示）

你在無頭取件會話，沒有人會回你的問題。邊界（機械，不可越）：

- 不關單、不拉 todo——那兩件是使用者的動作。
- 推送（2026-08-28 暫時放寬）：交給 push-guard 判斷。推得出去就推 `task/<N>` 工作分支，絕不推 main；被 guard 擋下就停在 commit 狀態收尾回報，那是設計不是錯誤，不要繞過。
- 結晶寫進單（`task note`／`block`／`done` 的留言），對話會消失，單不會。
- 任何沒把握的點不要猜：`task block` 留下「卡在哪＋你的建議」，然後結束。
